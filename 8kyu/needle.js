function findNeedle(haystack) {
    //return the phrase 'found the needle at position'
    // use .indexOf to return the index position of 'needle'
  return "found the needle at position " + haystack.indexOf("needle")
}